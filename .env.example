# PostgreSQL connection string (use "db" when running in Docker, "localhost" for host)
DB_DSN=postgres://risktracker:risktracker@db:5432/risktracker?sslmode=disable

# JWT secret key (change in production!)
JWT_SECRET=JwRTssvh2fasI8dnsahVlSAxsaffsascW21Vfb1tgMBsa

# Server port (default: 8080)
PORT=8080

# Environment: dev | prod (default: dev)
ENV=dev
